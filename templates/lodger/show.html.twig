{% extends 'base.html.twig' %}

{% block title %}Lodger{% endblock %}

{% block body %}
    <h1>Lodger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lodger.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ lodger.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ lodger.lastname }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ lodger.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ lodger.email }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ lodger.startDate ? lodger.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ lodger.EndDate ? lodger.EndDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lodger_index') }}">back to list</a>

    <a href="{{ path('lodger_edit', {'id': lodger.id}) }}">edit</a>

    {{ include('lodger/_delete_form.html.twig') }}
{% endblock %}
